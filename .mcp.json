{
  "mcpServers": {
    "debugium": {
      "command": "debugium",
      "args": ["mcp"],
      "description": "Debugium — live debugger MCP bridge. Inspect paused programs, step code, set breakpoints, annotate the UI, and record findings. Server must be running (`debugium launch <script>`) before tools are usable."
    }
  }
}
